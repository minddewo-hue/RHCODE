Use exactly vllm/gemma-4-31b-it-uncensored-bf16 and follow D:\work_space\DouYinFrida\AGENTS.md. Perform the final controlled live test yourself; do not modify code unless this test proves a concrete bug. The user explicitly authorizes one image to the test-account conversation `0:1:84845073037:1480416347556311`, previously associated with the authorized friend `静无声`, and arbitrary test messages are allowed. Frida is already forwarded at 127.0.0.1:27042, Douyin is running, and `/data/local/tmp/test_image.png` exists. Do not use UI automation, restart Frida, or start a separate listener. Run one `demo.py --host 127.0.0.1:27042` process with piped/controlled stdin: wait until READY, write exactly one `send-image 0:1:84845073037:1480416347556311 /data/local/tmp/test_image.png`, flush, keep that same process alive about 5 seconds so it can capture callbacks, then write `quit` and collect stdout/stderr. Keep the entire shell call under 12 seconds. Capture and report the real `IMAGE_PREP`, `DELIVERY_INITIATED`, `IMAGE_SEND_ACCEPTED`, and any `SENT`/`ERROR` events including dimensions, content_class, status, and field_errors. Do not send a second image if SENT is absent. Then run only targeted offline checks if code changed. For receive verification, inspect only events captured during this same run; if no incoming OnlyPictureContent arrives, state that the test account must send an image back. Clean the temporary probe/listen logs from prior Gamma runs but retain one concise final live-test log if useful. Finish with exact observed evidence, no inference beyond the log.
