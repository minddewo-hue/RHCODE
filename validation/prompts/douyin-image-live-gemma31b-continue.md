Continue the authorized live image test with exactly vllm/gemma-4-31b-it-uncensored-bf16. Follow the updated D:\work_space\DouYinFrida\AGENTS.md. The previous Gamma run already established that Douyin PID 29204 is running, `adb forward tcp:27042 tcp:27042` works, `frida-ps -H 127.0.0.1:27042 -ai` works, and `python douyin-im-demo\demo.py --host 127.0.0.1:27042 --listen 10` returned READY. Do not restart or rebind Frida and do not repeat failed connections to the ADB port. Remove only the temporary douyin-im-demo\probe.log and probe_v2.log created by the previous run. The user explicitly authorizes exactly one image to the test friend displayed as `静无声`, and no other recipient. Determine and verify that friend's exact conversation ID using a narrow existing hook/log lookup or the current Douyin UI; Android commands inside `adb shell` must use Android sh, while PowerShell filtering stays outside. Do not send until the displayed name and conversation are unambiguously matched. Use the plain green PNG at C:\Users\Administrator\Documents\xwechat_files\minddewo_7992\temp\InputTemp\80246c4e-0e0e-4ea8-a273-d16452beb145.png if present, push it to a readable Android path, start one listener, and use douyin-im-demo send-image to send exactly one image. Capture and report the Hook SENT image fields. Listen up to 60 seconds for a reply image from the same conversation; if none arrives, report receive-live verification pending without fabricating it. Fix only a proven douyin-im-demo bug and rerun targeted tests after any fix. Execute tools directly with valid PowerShell and finish concisely.
