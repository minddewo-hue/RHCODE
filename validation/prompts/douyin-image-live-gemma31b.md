Use exactly vllm/gemma-4-31b-it-uncensored-bf16 and follow D:\work_space\DouYinFrida\AGENTS.md. The user explicitly authorizes a live image-message test only with the Douyin friend whose displayed name is exactly `静无声`; this is the user's test account, and the user said arbitrary test messages are allowed. Work in D:\work_space\DouYinFrida, use the connected authorized ADB device and Douyin 39.5.0, and do the work yourself through tools. Do not send to any other conversation. First identify the exact conversation ID for `静无声` from the running app/hook or a narrowly scoped existing record and verify the name before sending. Use one small non-sensitive test PNG; prefer the plain green PNG at C:\Users\Administrator\Documents\xwechat_files\minddewo_7992\temp\InputTemp\80246c4e-0e0e-4ea8-a273-d16452beb145.png if it exists, push it to a readable Android path, then use douyin-im-demo's send-image RPC/CLI to send exactly one image. Capture the Hook `SENT` event and verify image.local_path, compress_path, mime, and field_errors. Keep the listener open for up to 60 seconds for an incoming image from the same conversation and verify the `RECEIVED` image fields if one arrives; if none arrives, report that receive-live verification still needs the test account to send an image back, and never fabricate it. Fix only concrete bugs in douyin-im-demo, never modify douyin39.5.0 or dytest, and rerun relevant offline tests after a fix. Use valid PowerShell commands only, no orchestration pseudocode, no broad recursive scan, and do not narrate future commands. Finish with the verified recipient, conversation ID (you may partially mask it in prose), actual sent-event evidence, receive result, fixes, and tests.
