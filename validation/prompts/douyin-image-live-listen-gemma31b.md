Use exactly vllm/gemma-4-31b-it-uncensored-bf16 and follow D:\work_space\DouYinFrida\AGENTS.md. This is a live receive-only diagnostic for the authorized test account `静无声`. Do not send any text, image, or file. Do not edit code yet. Do not restart or rebind frida-server. Refresh only `adb -s 192.168.11.67:5555 forward tcp:27042 tcp:27042`, verify `frida-ps -H 127.0.0.1:27042 -ai`, remove only stale final_live_receive.log and .err, then start exactly one background UTF-8 listener using `python -u douyin-im-demo/demo.py --host 127.0.0.1:27042 --listen 180`, redirecting stdout to `douyin-im-demo/final_live_receive.log` and stderr to `.err`. Poll no more often than every 5 seconds with separate short shell calls; never run Start-Sleep and never start another listener. Keep the desktop turn alive until the process exits or a RECEIVED image appears. On an image event, report the exact structured fields already emitted by the hook without binary data, then inspect the current hook only enough to identify any missing/failed fields. Do not apply a send fix and do not perform a live resend in this turn. If no image appears, report that exact result and stderr. Clean only obsolete prior probe/listen logs, retain final_receive.log and final_live_receive.log.
