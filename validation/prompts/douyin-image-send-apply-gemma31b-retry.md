Use exactly vllm/gemma-4-31b-it-uncensored-bf16 and follow D:\work_space\DouYinFrida\AGENTS.md. Continue your own accepted-without-SENT fix. The RHZYCODE Faker Provider now explicitly bridges `apply_patch` as a Codex custom tool and the desktop was rebuilt; prove the fix by editing through `apply_patch`, never shell text rewriting. Your prior patch proposal was not applied. Re-read only sendImageMessage and OnlyPictureContent if needed. Implement evidence-based actual image dimension extraction before building OnlyPictureContent, set width and height from decoded bounds, and emit preparation/delivery-initiated diagnostics without claiming SENT. Important: Frida access to Java instance fields on BitmapFactory.Options must also use `.value` (for example `inJustDecodeBounds.value`, `outWidth.value`, and `outHeight.value`); do not use plain JavaScript property assignment and do not hard-code dimensions. Reject undecodable dimensions with a staged error. Keep the patch narrow, preserve text behavior, and do not perform UI automation, Frida restart, or live send. Apply the patch once, verify the file actually contains the change, run node --check, py_compile, and all 7 unittests from douyin-im-demo, and update documentation only if the observable contract changed. Finish with exact changes and the remaining controlled live test. If apply_patch is still unsupported, stop immediately and report it without retrying.
