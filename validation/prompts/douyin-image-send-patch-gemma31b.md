Use exactly vllm/gemma-4-31b-it-uncensored-bf16 and follow D:\work_space\DouYinFrida\AGENTS.md. Continue your own diagnosis of the accepted-without-SENT image path. The RHZYCODE gateway custom-tool bug is now fixed, so `apply_patch` is available as a real custom tool; use it for edits and do not use shell-based text rewriting. Do not perform UI automation, restart Frida, or send a live message in this turn. Re-read only the relevant sendImageMessage block and exact OnlyPictureContent reference if needed. Your previous proposed hard-coded 1080x1920 dimensions are not acceptable because test images can have any size. If width/height are required, derive the actual Android image bounds safely (for example via platform image metadata) and reject undecodable images with a staged error; otherwise make the concrete evidence-based fix you determine. Preserve text behavior, keep picturePath/compressPath/mime/sendRaw semantics, add diagnostics that distinguish content preparation from asynchronous delivery, and do not treat builder acceptance as delivery. Update focused tests/docs for the actual behavior. Apply the patch, then run node --check douyin_im_demo.js, python -m py_compile demo.py test_demo.py, and python -m unittest -v test_demo.py with workdir douyin-im-demo. Finish with exact changes, evidence, tests, and the controlled live validation still required. Stop instead of guessing if the decompiled evidence cannot prove a fix.
