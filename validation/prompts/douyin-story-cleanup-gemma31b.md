Use exactly vllm/gemma-4-31b-it-uncensored-bf16 and follow D:\work_space\DouYinFrida\AGENTS.md. This is a cleanup-only task after your previous partial patch. Do not implement StoryPictureContent yet. Inspect only the last 120 lines of `douyin-im-demo/douyin_im_demo.js`. The legitimate file ends at the closing `};` of `rpc.exports`. Everything after that was incorrectly appended by the failed attempt: `function parseStoryPictureJson`, `function getStoryContentShape`, and a top-level `if (contentClass && contentClass.endsWith('.StoryPictureContent'))` block. Use exactly one apply_patch update with forward-slash path `douyin-im-demo/douyin_im_demo.js`, anchored on the final `};\nfunction parseStoryPictureJson`, to delete all content after the legitimate `};`. Wait for its output. Then verify the file ends at `};`, rg finds none of `parseStoryPictureJson|getStoryContentShape|StoryPictureContent`, and run node --check. After those succeed, use a separate apply_patch call to delete `douyin-im-demo/tests/test_parsing.js`, which is a fake test that duplicates production logic. Verify deletion and run `python -m py_compile douyin-im-demo/demo.py` plus `python -m unittest discover -s douyin-im-demo -p 'test_*.py'`. All calls must be serial. Do not touch any other files, do not attach Frida, and do not send anything. Report only observed cleanup and test results.
