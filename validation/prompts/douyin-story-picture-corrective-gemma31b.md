Use exactly vllm/gemma-4-31b-it-uncensored-bf16 and follow D:\work_space\DouYinFrida\AGENTS.md. Correct your previous failed delivery yourself. The prior main apply_patch failed with `Failed to find context`, so `douyin-im-demo/douyin_im_demo.js` currently contains no `StoryPictureContent`, `inspectStoryPictureContent`, or `raw_content_shape` implementation. Only `douyin-im-demo/tests/test_parsing.js` was added, and it incorrectly tests a duplicated `inspectStoryPictureContentMock` rather than production code. Do not claim completion from your prior answer. All tool calls must be strictly serial: wait for each tool result before issuing the next call. First inspect only narrow line ranges around declarations, inspectRemoteImageUrl/inspectImageContent, inspectMessage raw-content handling, and Java initialization; do not print entire files or the captured log. Then apply one small production patch through apply_patch. Verify the production symbols with rg and node --check before touching tests. Only after that, replace the fake test with a focused test that executes the actual production helper code (for example by evaluating the pre-initialization portion in a Node vm with safe mocks); never copy production parsing logic into the test. The live evidence is: msg_type 27; class `com.ss.android.ugc.aweme.im.sdk.chat.model.StoryPictureContent`; width 1080; height 2412; sendRaw false; very large `inline_pic` beginning `UklGR`; nested `resource_url` with data_size, md5, oid, origin/large/medium/thumb URL lists. Implement a non-null image object with local_path, compress_path, mime, width, height, send_raw/use_origin_image, deterministic remote_url priority, data_size, oid, md5, and bounded field_errors. Never emit inline_pic bytes/Base64, inline_sign, or unbounded raw image JSON. Emit a bounded `raw_content_shape` with key/type/size metadata; preserve ordinary text, OnlyPictureContent, auto-reply, and echo behavior. Malformed/missing fields must not throw. Initialize the StoryPictureContent Java class. Run in order after successful edits: node --check, the actual production-helper test, python -m py_compile, then all existing Python tests. Do not attach/restart Frida and do not send anything. Finish only after re-reading exact rg evidence and test outputs.
