Use exactly vllm/gemma-4-31b-it-uncensored-bf16 and follow D:\work_space\DouYinFrida\AGENTS.md. You own this implementation; the supervising agent must not implement the Douyin feature. A live receive-only run captured an authorized image from `静无声` with conversation_id `0:1:84845073037:1480416347556311`, msg_type 27, content_class `com.ss.android.ugc.aweme.im.sdk.chat.model.StoryPictureContent`, content_hint `[图片]`, and field_errors `[]`, but the emitted `image` field was null. The JSON raw content contained `cover_width:1080`, `cover_height:2412`, `from_gallery:1`, a very large `inline_pic` beginning with the WebP Base64 prefix `UklGR`, `inline_sign`, `md5`, `use_origin_image:false`, and nested `resource_url` with `data_size`, `md5`, `oid`, and origin/large/medium/thumb URL lists. The current log incorrectly emitted the entire inline_pic Base64 inside raw_content, causing a huge log and model context. Do not read or print the full final_live_receive.log line. Inspect only douyin-im-demo/douyin_im_demo.js and narrow exact decompiled references for StoryPictureContent/OnlyPictureContent. Implement robust structured receive parsing for StoryPictureContent while preserving OnlyPictureContent and text behavior. A StoryPictureContent event must emit a non-null image object with available width, height, mime, local_path, compress_path, send_raw/use_origin_image, remote_url chosen by a documented deterministic priority, data_size, oid, md5, and field_errors. Never emit inline_pic bytes or Base64, inline_sign, or the original unbounded image raw_content; instead emit a bounded raw_content_shape containing safe key/type/size metadata sufficient for diagnostics. Ensure malformed/missing nested fields add bounded field_errors rather than throwing. Add focused unit-testable helpers and tests using a tiny synthetic StoryPictureContent fixture (never copy the captured binary or signed URLs). Edit only douyin-im-demo via apply_patch. Run node --check, python -m py_compile, and all douyin-im-demo tests. Do not attach Frida, do not restart it, and do not send any live text/image/file. Finish with exact changes, tests, and one receive-only validation step.
