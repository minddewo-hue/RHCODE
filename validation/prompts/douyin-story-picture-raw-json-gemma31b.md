Use exactly vllm/gemma-4-31b-it-uncensored-bf16 and follow D:\work_space\DouYinFrida\AGENTS.md. You must implement this yourself. Previous attempts failed because a single apply_patch ordered hunks top, bottom, then middle. Do not reuse those patches or guess StoryPictureContent obfuscated Java methods. Work strictly serially and use separate apply_patch calls, each containing exactly one hunk in one location. The current production file remains unchanged and contains `function inspectMessage` at line ~133; it reads `message.getContent()` after inspecting the Java content object and currently always emits the unbounded raw string. Implement StoryPictureContent receiving from its raw JSON, which is the proven stable serialized contract. First add pure helpers immediately before `function inspectMessage`: parse bounded Story picture JSON; select remote URL deterministically in priority order `origin_url_list`, `large_url_list`, `medium_url_list`, `thumb_url_list`; infer WebP from an `inline_pic` starting `UklGR`; return local_path, compress_path, mime, cover_width/height as width/height, send_raw/use_origin_image, selected remote_url plus its source, data_size, oid, and md5; collect bounded errors without throwing. Add a separate pure raw-content shape helper that returns only key/type/length metadata, never values for `inline_pic`, `inline_sign`, URL strings, or other long strings. Verify that first patch applied with rg and node --check before continuing. Then use a second one-hunk patch only in the existing rawContent try block: after `message.getContent()`, when `contentClass` ends with `.StoryPictureContent`, populate `image` from the raw helper, set `text='[Image]'`, keep content_hint, create `rawContentShape`, and set the emitted raw content to null so Base64 is never logged. Preserve the existing text parsing for non-Story messages. Use a third one-hunk patch in the data object to add `raw_content_shape`. Do not add or initialize a StoryPictureContent Java class because raw JSON plus the already captured class name is sufficient. After production rg and node --check succeed, replace `tests/test_parsing.js`; the test must execute the actual helper functions from `douyin_im_demo.js` using Node `vm` and a tiny synthetic fixture, and assert URL priority, width/height, non-null image, Base64 omission, bounded shape, and malformed JSON behavior. It must not contain a copied parser implementation. Run tests serially: node test, py_compile, then `python -m unittest discover -s douyin-im-demo -p 'test_*.py'`. Do not attach/restart Frida or send anything. Do not announce completion until every apply_patch output and every test output has been observed.
